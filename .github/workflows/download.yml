name: Download client

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  downloader:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24.11
